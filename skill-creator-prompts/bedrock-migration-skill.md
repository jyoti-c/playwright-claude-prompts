/skill-creator Develop a skill that will check our AWS Bedrock model version used in the project. If the model is deprecated, replace it with the latest available model having similar price and efficiency. The skill should scan the project files, query the AWS Bedrock API for model status, find the best replacement based on a scoring algorithm (same provider, family, and capability tier), and finally report and apply the replacement.
